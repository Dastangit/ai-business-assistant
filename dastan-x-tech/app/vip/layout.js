export const metadata = {
  title: 'Acceso Privado VIP | DASTAN X-TECH',
  robots: {
    index: false,
    follow: false,
    nocache: true,
  },
};

export default function VipLayout({ children }) {
  return children;
}
